{
  "name": "Aquarium Manager Backend API",
  "description": "Backend API for the Aquarium Management System (Jakarta EE, EclipseLink)",
  "repository": "https://github.com/your-username/aquarium-manager-backend",
  "keywords": ["java", "jakarta-ee", "postgresql", "api", "aquarium"],
  "env": {
    "JWT_SECRET": {
      "description": "JWT secret key for token signing",
      "generator": "secret"
    },
    "JWT_EXPIRATION": {
      "description": "JWT token expiration time in milliseconds",
      "value": "86400000"
    }
  }
}